<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shihuo.shihuo">

    <application
        android:name=".application.ShiHuoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.BaseActivity" />
        <activity android:name=".Activities.SettingActivity" />
        <activity android:name=".Activities.FavGoodsListActivity" />
        <activity android:name=".Activities.FavShopsListActivity" />
        <activity android:name=".Activities.FavVideoListActivity" />
        <activity android:name=".Activities.FavServiceListActivity" />
    </application>

</manifest>